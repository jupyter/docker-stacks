# ReadTheDocs environment contains old package versions preinstalled
# So, to ensure we have modern packages, we pin minimum versions of some packages we need
# doit and plumbum are used to launch doit tasks
docutils>=0.17
doit
myst-parser>=0.16.1
plumbum
sphinx>=4.4.0
sphinx-book-theme>=0.3.2
sphinx-copybutton>=0.5.0
